# Keep this directory in git. nerves_defconfig sets BR2_GLOBAL_PATCH_DIR to
# include this dir (see line 17); Buildroot expects it to exist even when there
# are no patches yet. Drop patch files here as <pkg>/<name>.patch.
